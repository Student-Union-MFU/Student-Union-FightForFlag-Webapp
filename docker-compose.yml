services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: yion
      POSTGRES_PASSWORD: yion
      POSTGRES_DB: voteapp
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: